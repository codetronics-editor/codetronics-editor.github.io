

if(true&&1){compile.performParse(groupnode,a*((b)));true;}else if(2){}else{x;}var x;utils.forMap(grammar.token,funnnntion(val,key,i)); {
val.reference = i + 1;
if(i) {
pattern[0] += "|";}
pattern[0] += "(";
compile.buildRegexp(pattern,val.pattern);
pattern[0] += ")";
};


